import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    color: 'white',
    // position : 'absolute',
    // top : 40,
    fontSize : 40,
    fontFamily : 'Roboto'
  }
});

export default styles;